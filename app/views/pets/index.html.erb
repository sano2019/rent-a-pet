<h2>Pets</h2>

<ul class="list-group">
<% @pets.each do |pet| %>
  <li class="list-group-item"><%= link_to pet.name, pet %> : <%= pet.pet_category  %> </li>

  <% end %>
</ul>
<br>
<%= link_to "Create a pet", new_pet_path %>
